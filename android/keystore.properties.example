# Copy to android/keystore.properties (gitignored), or provide the equivalent
# ANDROID_UPLOAD_* environment variables in release CI.
storeFile=/absolute/path/to/groundwork-pro-upload.jks
storePassword=replace-me
keyAlias=groundwork-pro-upload
keyPassword=replace-me
